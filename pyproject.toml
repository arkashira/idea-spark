[project]
name = "idea-spark"
version = "0.1.0"
description = "Prompt library and customization for idea generation"
authors = [{name = "Axentx"}]
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
